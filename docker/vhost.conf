<VirtualHost *:80>
    DocumentRoot /var/www/html
    ServerName localhost

    <Directory /var/www/html>
        AllowOverride All
        Require all granted
    </Directory>

    <DirectoryMatch "^/var/www/html/(config|core)/">
        Require all denied
    </DirectoryMatch>
</VirtualHost>